from turtle import *
from turtle_font import *
global scale

global scale

setScale(25)
penup()
backward(300)

K()
E()
N()

exit()
for s in range(10):
    for r in range(2):
        forward(100)
        right(60)
        forward(100)
        right(120)
    right(36)

done()
